import { View, Text, FlatList } from 'react-native';
import React from 'react';

const ExpenseLists = () => {
  return (
    <View>
      <FlatList />
    </View>
  );
};

export default ExpenseLists;
